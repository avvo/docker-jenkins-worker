worker:
  image: registry.docker.prod.avvo.com/jenkins-worker
  ports:
    - "32403:22"
  links:
    - db:mydb
    - redis:myredis
    - memcached:mymemcached

db:
  image: mysql
  environment:
    MYSQL_ROOT_PASSWORD: root
  volumes:
    - /mnt/ramdisk/mysql3:/var/lib/mysql

redis:
  image: redis

memcached:
  image: memcached
